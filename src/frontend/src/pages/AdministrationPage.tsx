import { PlaceholderPage } from './PlaceholderPage';

export function AdministrationPage() {
  return (
    <PlaceholderPage
      title="Users, roles & permissions"
      owner="Võ Hoàng Minh (C)"
      useCases={[
        'UC15 — Manage users, roles and permissions',
        'UC16 — Sign in and sign out',
      ]}
    />
  );
}
