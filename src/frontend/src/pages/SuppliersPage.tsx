import { PlaceholderPage } from './PlaceholderPage';

export function SuppliersPage() {
  return (
    <PlaceholderPage
      title="Suppliers & goods receipts"
      owner="Võ Việt Tiến (A)"
      useCases={[
        'UC02 — Manage suppliers',
        'UC03 — Record a goods receipt',
      ]}
    />
  );
}
